/**
 * Root app/page.tsx — this file is superseded by app/(site)/page.tsx
 * which handles the "/" route through the (site) route group.
 * This file exists only as a safety guard and passes through to the site layout.
 */
export { default } from "./(site)/page";
